import java.util.Scanner;

public class FizzBuzz {
    public static void main(String[] args) {
        /*for (int i = 1; i <= 100; i++) {
            if (i%3==0 ){
                System.out.println(i + " fizz ");
            }
            if (i%5==0){
                System.out.println(i + " buzz ");
            }
            if (i%3==0 && i%5== 0) {
                System.out.println(i + " fizzbuzz ");*/

        //problem 3
        //integer array of size 100
        //each integer between 1 and 100 represented once
        //except one is missing
        //and one is duplicated
        /*for (int i = 1; i <= 100; i++) {
            System.out.println(i);
        }*/

        /*problem 2
        test a string to see if it is a palindrome
        level, a man a plan a canal panama
        go hang a salami i'm a lasagna log*/

         /*String original, reverse = "a man a plan a canal panama";
         Scanner scanner = new Scanner(System.in);
        System.out.println("Enter a string to test if it is a palindrome");
        original=*/

        /*String pal = "level";
        boolean isPal = true;
        for (int i = 0; i < pal.length(); i++) {
            if (pal.toCharArray()[i] != pal.toCharArray()[pal.length() - (i + 1)]) {
                isPal = false;
            }
        }
        System.out.print("The string is ");

        if (!isPal) {

            System.out.print("not ");

        }

        System.out.println("a palindrome.");*/

      /*  String man = "amanaplanacanalpanama";
        boolean isMan = true;
        for (int i = 0; i < man.length(); i++) {
            if (man.toCharArray()[i] != man.toCharArray()[man.length() - (i + 1)]) {
           isMan = false;
            }
        }
        System.out.println();
    }
}*/






        /*problem 4
        Christmas tree
           *
          ***
         *****
        ********

         */
        int treeSize = Utils.getNumber ("How tall is the tree?");
        printTree (treeSize);

        for (int row = 0; row < ; row++) {

        }







